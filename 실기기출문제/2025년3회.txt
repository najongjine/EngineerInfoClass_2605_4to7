1. 다음은 UML (    ) 다이어그램이다. 아래 내용을 보고 다이어그램의 관계를 확인하여 명칭을 작성하시오. 
패키지 다이어그램
 - 폴더 모양의 단위로 구분하여 구성 요소 간의 관계를 표현하는 UML 구조 다이어그램이다.
 - 코드의 실제 구조(폴더 구조)와 비슷하게 표현되기 때문에 소프트웨어의 모듈화, 재사용성, 의존 관계를 시각적으로 설계할 때 자주 사용된다.



2. 다음은 소프트웨어 테스트 기법 중 하나에 대한 설명이다
조건(Condition) 커버리지
 - 결정문(예: if (A && B) 또는 if (X > 10 || Y == 0))
    각각의 조건식이 True와 False 두 가지 경우를 모두 한 번 이상 만족하도록 테스트 케이스를 설계해야 한다.
 - 그렇다고 해서 전체 결정식(Decision Expression) 의 결과(True/False)가 모두 수행된다고 보장하지는 않는다.



3. 다음은 유닉스(Unix) 또는 리눅스( Linux) 환경에서 자주 사용하는 기본 명령어에 대한 설명이다.
ls
- 디렉터리의 내용(파일 및 하위 디렉터리)을 목록으로 표시한다. 
cd
- 다른 디렉터리로 이동한다
cp
- 파일을 복사한다.
pwd
- 현재 작업 중인 디렉터리의 경로를 출력한다.



4. 다음은 오류검출 방식을 설명하는 내용이다. 설명의 빈칸 (①)~(⑤) 에 들어갈 알맞은 용어를 <보기>에서 고르시오.
(Hamming) 코드는 전송 데이터에 여러 개의 검사 비트를 추가하여 오류를 검출하고 수정까지 가능한 방법이다.
이 코드는 재전송 없이 수신 측에서 자체 수정하는 (FEC) 방식에 속한다.

이에 반해 오류 발생 시 송신 측에 재전송을 요구하는 방식은 (BEC)이라 하며, 여기에 포함되는 대표적 검출 기법으로 (④) 검사와 (⑤) 검사가 있다.
(Parity) 검사는 데이터 블록 끝에 1비트 검사 비트를 추가하여 오류를 검출한다.
(CRC) 검사는 송신측과 수신측이 동일한 특정 다항식을 사용하여 오류를 검출한다.




5. 다음은 C코드에 대한 문제이다. 아래 코드를 확인하여 알맞는 출력값을 작성하시오.
#include <stdio.h>

struct Test {
    int i;
    const char* g;
};
int main() {
    /* C언어는 문자열을 파이썬처럼 쉽게 못다룸. 문자열은 무조건 heap영역에 공간 만들고 시작주소를 따로 저장 해야함 */
    struct Test test[] = { {1, "AB"}, {2, "DC"}, {3, "EB"} };
    struct Test* p = &test[1];
    printf("%s", p->g + (p->i - 1));
    return 0;
}

---------------------------
6. 다음은 C코드에 대한 문제이다. 아래 코드를 확인하여 알맞는 출력값을 작성하시오.
#include <stdio.h>

int main(void) {
    char str[] = "REPUBLICOFKOREA";
    int a = 0; // 13
    /*
    * str[0] != '\0'
    * str[1] != '\0'
    * str[2] != '\0' ....
    * a 가 15가 되면 a[15] == \0 즉 while문 빠져나옴
    */
    while (str[a] != '\0')
        ++a;

    printf("%d \n", a);
    // str[13]
    putchar(str[a - 2]); // putchar := 문자 하나만 출력. 다음줄 개행 없음
    return 0;
}

-------------------------------------------

7. 다음은 C코드에 대한 문제이다. 아래 코드를 확인하여 알맞는 출력값을 작성하시오.
#include <stdio.h>

struct Node {
    struct Node* next;
    unsigned int x;
};

int main() {
    struct Node t1 = { 0, 5u };
    struct Node t2 = { 0, 7u };
    struct Node t3 = { 0, 11u };

    t3.next = &t2;
    t2.next = &t1;

    struct Node* curr = &t3;
    int sum = 0;

    while (curr) {
        sum = sum * 3 + curr->x;
        curr = curr->next;
    }
    //         xor 연산    + 100
    sum = (sum ^ 42u) + 100u;

    printf("%u\n", sum);
}

-------------------------------

9. 다음은 파이썬에 대한 문제이다. 아래 코드를 확인하여 출력값에 알맞는 값을 작성하시오. 
data = [
    [3, 5, 2, 4, 1],
    [4, 5, 1],
    [4, 4, 1, 5, 4],
    [4, 5]
]
 
result = {}
"""

data:                [
                            [3, 5, 2, 4, 1],
                            [4, 5, 1],
                            [4, 4, 1, 5, 4],
                            [4, 5]
                     ]
lis = [3, 5, 2, 4, 1]
       [4, 5, 1]
       [4, 4, 1, 5, 4]
       [4, 5]
"""
for index, lis in enumerate(data):
    list_sum = sum(lis) # 15
    list_len = len(lis) # 5
    """
       result[0] = (15,5)
       result[1] = (10,3)
       {0: (15, 5), 1: (10, 3), 2: (18, 5), 3: (9, 2)}
    """
    result[index] = (list_sum, list_len)
 
print(result)


-------------------

11. 다음 설명에 해당하는 인증 기술을 쓰시오.
OTP (One-Time Password)
 - 한 번 사용하면 폐기
 - 시간 동기화 기반
 - 매번 새로운 값 생성
 - 은행 인증, 고보안



13. 다음은 인증 및 자원 접근 방식에 대한 설명이다. 알맞은 단어를 작성하시오.
OAuth  :  구글 로그인으로 chatgpt 로그인 하는거
 - 사용자가 새로운 사이트에 가입하지 않고 평소에 이용하던 서비스의 계정으로 로그인할 수 있게 해주는 기술이다.
 - 소셜 로그인
 - 동일 시스템 내 인증이 아니라 서로 다른 서비스 간의 권한 위임



15. 다음은 C코드에 대한 문제이다. 아래 코드를 확인하여 알맞는 출력값을 작성하시오.
#include <stdio.h>
int main() {
    int x = 7, y = 4, z;
    //  1 < 3 ? 2:1  -> 2
    z = y % 3 < 3 ? 2 : 1;
    //z = z & (z >> 1);
    //  10 >> 1 == 1,  10 & 01 == 0
    z = z & z >> 1;
    // (x > 5) && (z <= 3) ? (z * x) : (z / x);
    // 7>5 && 0<=3
    // t && t = t  -> z * x -> 0 * 7
    z = x > 5 && z <= 3 ? z * x : z / x;
    printf("%d", z);
    return 0;
}

-------------------------------

16. 관계형 데이터베이스 개념에 대한 설명이다. 빈칸에 들어갈 용어를 <보기>에서 골라 순서대로 쓰시오.
튜플
- 테이블에서 한 행(Row)을 의미하며, 하나의 레코드를 구성하는 요소
인스턴스
- 실제 데이터가 저장되어 있는 테이블의 내용 전체를 의미하며, 데이터의 상태를 나타낸다.
카디널리티
- 테이블에 저장된 행(Row)의 총 개수를 의미한다.




17. 다음은 Java에 대한 코드이다. 알맞는 출력값을 작성하시오. 
package dummy;

enum Test{
	A,B,C
}

enum Tri {
    A("A"), B("AB"), C("ABC");
 
    private String code;
 
    Tri(String code) {
        this.code = code;
    }
 
    public String code() {
        return code;
    }
}
public class Test11 {	
	public static void main(String[] args) {
		/*
		Tri t1 = Tri.A;
        Tri t2 = Tri.B;
        Tri t3 = Tri.C;

        System.out.println(t1);        // A
        System.out.println(t1.code()); // A

        System.out.println(t2);        // B
        System.out.println(t2.code()); // AB

        System.out.println(t3);        // C
        System.out.println(t3.code()); // ABC
        */
        // Tri.A.name() -> A -> A.length() -> 1
        // Tri.values()[1] ->  B("AB")
        Tri t = Tri.values()[Tri.A.name().length()];
        System.out.print(t.code());
	}
}

----------------------------

18. 다음은 정보보안에서 사용하는 접근통제(Access Control) 방식에 대한 설명이다.

설명에 해당하는 접근통제 모델을 <보기>에서 골라 빈칸에 작성하시오.
DAC     
- 소유자 기반
- 자원의 소유자(Owner)가 해당 자원에 대한 접근 권한을 자유롭게 부여하거나 회수할 수 있는 방식이다. 파일이나 폴더의 소유자가 읽기/쓰기/실행 권한을 설정하는 것이 대표적인 예로 사용자의 임의 설정이 가능해 보안성이 상대적으로 낮다.
MAC      
- 군대 비슷
- 	중앙에서 보안 정책을 일괄적으로 설정하며, 주체(사용자)가 임의로 수정하거나 변경할 수 없다. 주로 군사 기밀, 국가 보안과 같은 높은 보안 수준이 요구되는 환경에서 사용된다. 보안 등급(Top Secret / Secret / Confidential 등)에 따라 접근 여부가 결정된다.
RBAC
- 역할(Role)
- 조직 내에서 부여된 직무나 역할(Role)에 따라 접근 권한을 부여하는 방식이다. 개별 사용자에게 직접 권한을 설정하지 않고, 역할에 권한을 묶어 부여하기 때문에 관리가 용이하며 직무 변경 시 역할만 변경하면 된다.